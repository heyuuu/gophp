package zend

import "time"

func Sleep(time time.Duration) time.Duration {
	// todo 实现 sleep 逻辑
	// 正常 sleep 返回 0，异常中断返回剩余 sleep 时间
	return 0
}

func SleepUtil(time time.Time) time.Duration {
	// todo 实现 sleep 逻辑
	// 正常 sleep 返回 0，异常中断返回剩余 sleep 时间
	return 0
}
